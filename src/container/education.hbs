<div>
    {{#with main}}
    <main>
        <section>
            {{>title data='My education' big=true }}            
           <em>({{gender}})</em>

        <section>
            {{>title data='My certificates' big=false }}
            <ul>
                {{#each certificates }}
                    <li>
                        {{name}}
                        ({{id}})
                    </li>
                           
                {{/each}}
            </ul>
        </section>
    </main>
{{/with}}

</div>